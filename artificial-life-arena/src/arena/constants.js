export const operations = [
    on => on.x -= 1,
    on => on.y -= 1,
    on => on.y += 1,
    on => on.x += 1
]

export const DIE = -10
export const NOTHING = 0
export const SAME = 0
export const FOOD = 5